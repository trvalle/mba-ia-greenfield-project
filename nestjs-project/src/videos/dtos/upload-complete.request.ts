/**
 * Request DTO for POST /videos/{id}/upload-complete
 * The request body is empty; validation happens via path parameters.
 */
export class UploadCompleteRequest {
  // Empty body intentional — all validation is via path parameter
}
